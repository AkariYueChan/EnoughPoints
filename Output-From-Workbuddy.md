# 选课学分规划 · 拖拽小游戏（单文件 HTML）

## 目标
基于 `/.xlsx`（子表「课程设置总表」）做一个**单文件、零依赖**的 HTML 页面：
所有课程以小卡片形式摆在页面上的「待选课程池」里，用户把卡片拖进 **公共基础课** / **专业课** 两个框框，
每个框实时累加学分；未达标 → 边框标红，达标或超额 → 边框变绿。

## 数据（已从表格核对完成）

| 类别 | 课程数 | 学分 | 目标线 | 达标条件 |
|---|---|---|---|---|
| 公共基础课（专科） | 26 门（全部必修） | 49 | **49** | 26 门全部选入（表头原文 58 与实列 49 不符，按用户确认取 49） |
| 专业课 | 37 门 = 必修 16 + 选修 21 | 必修 74 / 选修 69 / 合计 143 | **89** | **16 门必修全部选入** 且 总学分 ≥ 89（即选修至少取到 15 分） |
| 合计 | 43 门 | 192 | — | — |

- 数据源：子表 `000001`「课程设置总表」，课程行 3–28（公共基础课）、30–66（专业课）；列 C 课程编号 / D 课程名称 / F 课程属性 / G 必修选修 / H 学分 / N 开设学期。
- 专业课的 `课程属性` 有 4 类：专业核心课、专业拓展课、专业群平台课（专业基础课）、专业综合技能（含实践）课。
- 学分含 0.2 / 0.5 / 3.5 等小数（如 5 门「形势与政策」各 0.2），累加需按 `Math.round(x*10)/10` 消除浮点误差。

## 交付物
`/main.html`
（内联 CSS + 原生 JS，无 CDN、无构建，双击即可用）

## 页面结构
1. **顶部栏**：标题 + 总进度条（`已完成 0 / 2 个类别`、已选总学分 / 192）+ 「重置」按钮。
2. **两个类别框**（左右并排，各占一半）：
   - 框头：类别名 + `已选 X / 目标 Y 学分` + 状态徽标（`还差 N 分` / `已达标 ✓`）。
   - 专业课框额外显示两条进度：`必修 0/16 门`、`选修 ≥15 分`。
   - 框体是放置区（dashed 边框），内部卡片流式排布，超高时框内滚动（`max-height: 44vh`）。
   - 框下方显示该类别已选课程的学分小计。
3. **待选课程池**（在两个框下方，占满宽度）：剩余卡片自动流式排布；上方带筛选小标签
   `全部 / 公共基础课 / 专业必修 / 专业选修`（43 张卡较多，纯便利功能）。

## 卡片设计
- 内容：课程名称（两行截断）+ 学分数字（大号）+ 标签行 `必修/选修` 徽标 + 课程属性 + 开设学期。
- 公共基础课用蓝色系、专业课必修用琥珀色、专业课选修用紫色系，便于一眼区分。
- 状态：`dragging` 半透明上浮；放进框后卡片上显示所属类别色边框。

## 交互
- **拖拽**：HTML5 DnD（`draggable=true`；`dragstart` 写入课程 id；放置区 `dragover` preventDefault + 高亮；`drop` 后重新计算）。
  支持三向移动：池 → 框、框 → 池、框 ↔ 框。
- **点击兜底**（同时兼容触屏 / 不想拖的场景）：点池里的卡片 → 进入它所属类别的框；点框里的卡片 → 退回课程池。
- **重置**：清空两个框，所有卡片回到池中（按原始编号顺序）。

## 达成判定与配色（每张卡片变化后立即重算）
- **公共基础课**：`已选门数 === 26` 且 学分 = 49 → 绿；否则红，提示 `还差 N 学分 / 还差 M 门必修`。
- **专业课**：`16 门必修全在框内` 且 `总学分 ≥ 89` → 绿；否则红，提示分别给出 `必修还差 M 门`、`总学分还差 N 分`。
- 配色：绿 `#16a34a`（边框/框头底色/徽标），红 `#dc2626`（同上），中性 `#94a3b8`。
  （此处按用户明确要求「不足=红、达标=绿」，不套用涨红跌绿的行情约定。）
- 两个框同时变绿 → 顶部进度条变绿并弹出「全部达标」完成提示。

## 实施步骤
1. 从 xlsx 精确抽出 43 门课程 → JS 常量数组
   `{id, name, cat:'public'|'major', type:'必修'|'选修', attr, credit, term}`，并校验 公共基础课合计 49、专业必修 74、专业选修 69、总计 192。
2. 写 HTML 骨架 + CSS（浅色主题、圆角卡片、框体状态类 `.ok` / `.bad`）。
3. 写渲染逻辑：`state = { pool:[], public:[], major:[] }`，`render()` 全量重绘两个框与池。
4. 写 DnD 事件 + 点击兜底 + 重置。
5. 写判定函数 `evaluate()`，输出每个框的状态、差值文案、总进度。
6. 自检：用脚本统计 HTML 内嵌课程数与学分数是否与表格一致；再用浏览器打开预览确认布局与拖拽。

## 关键文件
- 新建：`/main.html`
- 只读参考：`/.xlsx`（子表「课程设置总表」）
